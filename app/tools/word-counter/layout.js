export const metadata = {
  title: "Contador de Palabras y Caracteres",
  description: "Cuenta palabras, caracteres, oraciones y párrafos al instante. Estima tiempo de lectura y tiempo hablando.",
};

export default function Layout({ children }) {
  return children;
}
